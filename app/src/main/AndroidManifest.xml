<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidnote">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidNote">
        <activity android:name=".task.TaskActivity"/>
        <activity android:name=".task.GHIActivity"
            android:launchMode="singleInstance"
            />
<!--            android:taskAffinity="com.example.testA"-->


        <activity android:name=".task.DEFActivity"
            android:taskAffinity="com.example.testB"/>

        <activity android:name=".task.ABCActivity"
            android:taskAffinity="com.example.testC"/>

        <activity android:name=".touchEvent.TouchEventCheckActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>